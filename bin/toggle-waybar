#!/usr/bin/env bash
# toggle-waybar — Super+B hide / show taskbar.
#
# In winmode the taskbar is waybar (strip) + winbar-dock (icons).
# Desired visibility is tracked as bar-hidden so a Super+B during
# winmode enter cannot lose to a delayed dock start.
set -u
exec "$HOME/.local/bin/winmode-chrome" toggle
